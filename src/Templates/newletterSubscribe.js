const baseUri = "http://localhost:3000";

export const subscribe = (id) => {
  return `    
     ${baseUri}/api/subscribe/${id}
    `;
};

export const unsubscribe = (id) => {
  return `
            
    
    `;
};
